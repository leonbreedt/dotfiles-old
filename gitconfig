[user]
	name = Leon Breedt
	email = ljb@bitserf.org
[alias]
        aa = add --all
	co = checkout
        ci = commit
        ca = commit -a
        lg = log --graph --pretty=format:'%Cred%h%Creset - %G? -%C(yellow)%d%Creset %s %Cgreen(%cr)%Creset <%C(cyan)%ae%Creset>' --abbrev-commit --date=relative --name-status
        st = status
[color]
        diff = auto
        status = auto
        branch = auto
        interactive = auto
        ui = true
        pager = true
[mergetool]
	keepBackup = true
[commit]
	gpgsign = true
